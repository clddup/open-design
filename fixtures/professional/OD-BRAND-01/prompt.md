Create an original identity mark for OpenDesign on a 960×640 artboard.

The mark must use a named, non-destructive Boolean group with editable Path and shape operands, not a raster image or a loose pile of root layers. Pair it with a restrained wordmark and descriptor, use precise alignment, gradients, stroke, glow, and depth, then make one concrete optical correction after inspecting the first rendered result.

This fixture is the stable source for later flatten, outline-stroke, SVG round-trip, and pixel-baseline acceptance. Do not replace the editable Boolean master with its derived renderer path.
